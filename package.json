{
	"name": "lab1",
	"version": "1.0.0",
	"description": "",
	"license": "ISC",
	"author": "",
	"type": "commonjs",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "concurrently \"npm --prefix packages/react-frontend run dev\" \"npm --prefix packages/express-backend run dev\""
	},
	"workspaces": [
		"packages/*"
	],
	"devDependencies": {
		"concurrently": "^10.0.5"
	}
}
